# 科研技能包 · Research Skills

面向科研工作的 agent skills：

- **experiment-design（实验设计与统计分析）**：覆盖采集前的设计、随机化、样本量/功效、DOE 和预注册，以及采集后按计划执行的验证性统计。

## 使用

将技能目录放入 agent 的 skills 目录（如 `.claude/skills/` 或 `.agents/skills/`）即可被发现。
若含 `requirements.txt`，按其安装脚本依赖。

## 许可

MIT，见 LICENSE。
